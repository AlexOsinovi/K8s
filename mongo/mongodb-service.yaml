# mongo/mongodb-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: payment-mongo
  namespace: innowise
spec:
  clusterIP: None
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: payment-mongo
