# K8s/api-gateway/api-gateway-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: innowise
spec:
  ports:
    - port: 8083
      targetPort: 8083
  selector:
    app: api-gateway
  type: NodePort