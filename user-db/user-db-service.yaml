# K8s/user-db/user-db-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: innowise
spec:
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: user-db